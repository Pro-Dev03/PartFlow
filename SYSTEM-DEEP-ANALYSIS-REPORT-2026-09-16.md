# تقرير التحليل العميق لنظام PartFlow

**تاريخ التحليل:** 2026-09-16  
**نطاق التحليل:** الواجهة الأمامية، تطبيق Electron، خدمة Go API، SQLite المحلي، PostgreSQL/الخادم السحابي، المزامنة، Worker، المصادقة، قاعدة البيانات، الاختبارات، والتشغيل والنشر.  
**حالة الشجرة أثناء التحليل:** يوجد عدد كبير من التغييرات المحلية غير الملتزمة في المستودع. هذا التقرير يصف الحالة الموجودة فعليًا وقت الفحص ولا يفترض أن كل التغييرات المحلية أُنشئت في جلسة واحدة.

## 1. الملخص التنفيذي

PartFlow هو نظام إدارة متجر ومخزون ومبيعات، موجه أساسًا إلى متجر قطع الغيار والإلكترونيات. البنية الحالية ليست تطبيقًا سحابيًا تقليديًا فقط؛ بل هي نموذج هجين:

```text
Electron / Browser UI
        |
        v
Go API محلي على localhost:8080
        |
        v
SQLite محلية للعمليات اليومية

المصادقة والتحقق من الاشتراك <----> Cloud API على Render/PostgreSQL

مزامنة يدوية Local SQLite <----> Cloud API
Worker مستقل للمهام الدورية على قاعدة الخادم
```

### التقييم العام

| المجال | التقييم | الملاحظة الرئيسية |
|---|---|---|
| تغطية وظائف المتجر | جيدة وواسعة | البيع، الشراء، المخزون، العملاء، الموردون، الديون، المصروفات، المرتجعات والتقارير موجودة |
| الواجهة والبناء | جيد | `tsc` و`vite build` يمران، واختبارات الواجهة الحالية تمر |
| تصميم Offline | موجود تشغيليًا | SQLite وElectron وطبقة مزامنة موجودة، لكن التشغيل يعتمد على تحقق سحابي قبل العمليات |
| المصادقة | قوية نسبيًا | JWT محلي مع CloudGuard والتحقق من الاشتراك والتعامل مع refresh tokens |
| اتساق قاعدة البيانات | متوسط إلى ضعيف | عدد كبير من migrations وطبقة schema توافقية ضخمة ترفع خطر الانحراف |
| المزامنة | جزئية ومقيدة | snapshot أولي وqueue push موجودان، لكن الصلاحية والـ conflict resolution يداران يدويًا وبصلاحيات Admin |
| الاختبارات | جيدة في الوحدات، غير مكتملة تشغيليًا | Backend يفشل حاليًا في اختبار واحد، ولا تغطي اختبارات E2E كل دورة العمل المالية |
| الجاهزية الإنتاجية | تحتاج ضبطًا | يلزم إصلاح اختبار SQLite، توثيق العقد الفعلية، والتحقق من العزل السحابي والمراقبة والنسخ الاحتياطية |

## 2. منهجية ومصادر التحليل

تم تتبع النظام من نقاط الدخول إلى التخزين، وليس من أسماء المجلدات فقط:

- نقطة تشغيل API في [backend/cmd/api/main.go](backend/cmd/api/main.go).
- تسجيل المسارات المركزي في [backend/internal/api/router.go](backend/internal/api/router.go).
- تكوين قاعدة البيانات في [backend/pkg/config/config.go](backend/pkg/config/config.go) و[backend/pkg/database/database.go](backend/pkg/database/database.go).
- إنشاء SQLite المحلي ومخططه التوافقي في [backend/internal/localdb/localdb.go](backend/internal/localdb/localdb.go).
- تشغيل التطبيق المكتبي وإدارة الـ sidecar في [frontend/electron/main.js](frontend/electron/main.js).
- مسارات الواجهة في [frontend/src/app/router/index.tsx](frontend/src/app/router/index.tsx)، وقائمة الأقسام في [frontend/src/components/navigation/sidebar.tsx](frontend/src/components/navigation/sidebar.tsx).
- عقود الاستدعاء من الواجهة في [frontend/src/services/api/endpoints.ts](frontend/src/services/api/endpoints.ts) وعميل HTTP في [frontend/src/services/api/client.ts](frontend/src/services/api/client.ts).
- اختبارات Backend وFrontend الحالية، إضافة إلى نتيجة تشغيل `go test ./...` و`npm run build:check` و`npm run test:run`.

> توجد تقارير قديمة في المستودع تصف مراحل سابقة أو حلولًا مقترحة. عند وجود تعارض، اعتمد هذا التقرير على الكود الحالي ومسار التنفيذ الفعلي.

## 3. المعمارية الفعلية

### 3.1 الواجهة الأمامية

الواجهة React 19 + TypeScript + Vite، وتستخدم:

- React Router للمسارات.
- TanStack Query لجلب البيانات وتحديثها وإبطال cache.
- Zustand للحالة المحلية مثل المصادقة وواجهة المستخدم.
- React Hook Form وZod للنماذج والتحقق.
- Recharts للتقارير والرسوم.
- i18next للغتين العربية والإنجليزية.
- PWA generation في build، مع دعم Electron desktop.

المسارات محملة lazy، والصفحات المحمية تعمل تحت `/app`. الوضع الافتراضي عربي واتجاه العرض RTL.

### 3.2 التطبيق المكتبي

Electron يشغل واجهة Vite ويبني Go executable كعملية محلية. في وضع Electron:

- الخدمة المحلية تعمل عادة على المنفذ `8080`.
- `DB_CONNECTION_MODE=local`.
- يتم تحديد مسار SQLite داخل user data، مثل `PartFlow/data/partflow.db`.
- يمكن نسخ قاعدة مرفقة أول مرة إلى المسار المحلي.
- يتم فحص `/health` قبل فتح الواجهة.
- توجد IPC handlers لحفظ وقراءة صور المنتجات وأنواع القطع والتصنيفات.
- يتم منع path traversal للصور عبر التحقق من معرّف الكيان وحصر الحجم في 5MB.

هذا يجعل Electron نقطة التشغيل العملية للـ Offline، وليس مجرد غلاف للواجهة.

### 3.3 Backend API

الخدمة مكتوبة بـ Go وGin وSQLX. نقطة الدخول تهيئ:

1. التكوين.
2. Structured logging.
3. اتصال قاعدة البيانات الأساسي.
4. SQLite المحلي.
5. health/readiness/liveness.
6. Middleware الأمن والتسجيل وRequest ID وRate Limiter.
7. المصادقة.
8. جميع الخدمات والمستودعات والمسارات.

التسجيل المركزي في [backend/internal/api/router.go](backend/internal/api/router.go) يجمع الأقسام في API versioned تحت `/api/v1`.

### 3.4 قاعدة البيانات

يوجد مساران مختلفان للتخزين:

- PostgreSQL/قاعدة الخادم للبيانات السحابية والمصادقة والـ Worker.
- SQLite محلية للتشغيل اليومي في desktop.

الفروقات الأساسية بين المحركين مجمعة جزئيًا في [backend/internal/database/dialect.go](backend/internal/database/dialect.go)، مثل `NOW()` مقابل `CURRENT_TIMESTAMP`، و`$1` مقابل `?`، وتحويل timestamps.

لكن التوافق ليس طبقة كاملة موحدة؛ توجد مستودعات كثيرة تحتوي على فروع SQLite خاصة واستعلامات PostgreSQL خاصة.

## 4. تحليل كل قسم وظيفي

### 4.1 المصادقة والحسابات

**الوظيفة:** التسجيل، تسجيل الدخول، refresh، logout، استرجاع كلمة المرور، جلسة Cloud إلى Local، والتحقق من المستخدم الحالي.

**التنفيذ:**

- كلمات المرور تخزن باستخدام bcrypt.
- access token قصير العمر وrefresh token أطول عمرًا.
- refresh token يخزن digest له في جدول `refresh_tokens` عندما تكون migration موجودة.
- يوجد fallback قديم لبعض قواعد PostgreSQL التي تستخدم `crypt`.
- حالة الاشتراك وتاريخ الانتهاء يراجعان أثناء تسجيل الدخول.
- في Local SQLite، لا تكفي بيانات المستخدم المحلية وحدها؛ يمر الطلب المحمي عبر CloudGuard ويتطلب `X-PartFlow-Cloud-Token` صالحًا.

**الحكم:** فصل القرار السحابي عن قاعدة التشغيل المحلية قرار أمني صحيح، لكنه يعني أن “Offline” هنا لا يعني تنفيذ عمليات مالية بلا إنترنت. عميل الواجهة يمنع mutation عند `navigator.onLine=false` أو عند فشل تحقق الاشتراك، رغم أن البيانات تحفظ محليًا.

### 4.2 لوحة التحكم والنشاط

**الوظيفة:** إحصاءات المبيعات والمشتريات والمصروفات والربح والمنتجات والعملاء والموردين، مخطط أداء، المخزون المنخفض، الديون المتأخرة، وآخر النشاط.

**التنفيذ:**

- الصفحة في [frontend/src/features/dashboard/pages/DashboardPage.tsx](frontend/src/features/dashboard/pages/DashboardPage.tsx).
- تستخدم React Query مع فترات إعادة جلب مختلفة.
- Backend يعالج حالات قاعدة فارغة في بعض endpoints بإرجاع صفر أو قائمة فارغة.
- توجد قواعد مركزية للديون المفتوحة في [backend/internal/business/debt_rules.go](backend/internal/business/debt_rules.go).
- حساب الربح عولج ليتوافق مع tax-adjusted returns في `daily_metrics.go`.

**المخاطر:** الصفحة تجمع بعض الأعداد مرة من dashboard ومرة من قوائم العملاء/الديون، ثم تختار `Math.max`. هذا يحسن تحمل اختلاف response shapes، لكنه يخفي أحيانًا اختلافًا حقيقيًا بين مصدرين بدل كشفه.

### 4.3 نقطة البيع والمبيعات

**الوظيفة:** إنشاء البيع، السلة، البحث بالاسم أو SKU أو barcode، الدفع النقدي/الآجل، حساب الباقي، تعليق البيع، إلغاؤه، تحديث دفع لاحق، والفواتير.

**المسارات:** `POST/GET /sales`، held sales، payment، cancel، summary، top-products، وprofit.

**التنفيذ:**

- واجهة POS في [frontend/src/features/sales/pages/POSPage.tsx](frontend/src/features/sales/pages/POSPage.tsx).
- مكونات حديثة للدفع والمنتجات في `features/sales/components/modern`.
- دعم `cash_received` و`change_amount`.
- منطق الحساب المالي في خدمة المبيعات، مع دعم SQLite/PostgreSQL.
- منع التكرار يعتمد جزئيًا على idempotency والمفاتيح الفريدة.

**نقاط يجب اختبارها باستمرار:** بيع قطعة serialized، بيع كمية، بيع آجل، دفع جزئي، إلغاء بيع، انقطاع الخدمة أثناء الحفظ، وبيع عنصر سبق إرجاعه.

### 4.4 المنتجات والتصنيفات وأنواع القطع

**الوظيفة:** إدارة المنتجات، SKU، barcode، العلامة التجارية، الفئة، السعر والتكلفة، الحد الأدنى للمخزون، warranty، tracking الفردي، وأنواع القطع وخصائصها.

**التنفيذ:**

- المنتجات والتصنيفات عبر `productsApi` و`categoriesApi`.
- `part_types` وspecifications لها وحدة Backend وواجهة مستقلة.
- صور المنتجات والتصنيفات تحفظ عبر IPC في Electron، لا داخل قاعدة البيانات مباشرة.
- أرشفة المنتج وsoft delete يحافظان على التاريخ عندما يكون للمنتج عمليات سابقة.

**الحكم:** المجال غني، لكن وجود حقول متراكمة مثل `purchase_price` و`cost_price` وحقول توافقية متعددة يتطلب عقدًا واضحًا يحدد الحقل المحاسبي المعتمد.

### 4.5 المخزون

**الوظيفة:** العناصر الفردية، الكمية، barcode، serial، الحالة، التكلفة، سعر البيع، المورد، الموقع، الحركات، الحجوزات، الجرد، والمخزون الافتتاحي.

**التنفيذ:**

- المسارات في `internal/inventory` و`inventoryApi`.
- SQLite تستخدم `inventory_items` مع جداول توافقية مثل `inventory`, `inventory_movements`, `reservations`.
- عامل الخلفية ينهي الحجوزات المنتهية.
- توجد اختبارات للكميات، supplierless lifecycle، opening stock، وSQLite timestamp.

**ملاحظة معمارية:** مفهوم “المنتج” المجمع ومفهوم “عنصر المخزون” الفردي كلاهما مستخدمان. يجب أن يبقى القرار واضحًا: الكمية المجمعة تستخدم للسلع غير الفردية، بينما barcode/serial والحالة والرجوع تحتاج `inventory_item_id`.

### 4.6 المشتريات والاستلام

**الوظيفة:** إنشاء وتعديل وعرض الشراء، إضافة بنود، الاستلام، الإلغاء، العكس، الدفع، والربط بالمخزون والمورد.

**المسارات:** `purchases`, `items`, `receive`, `cancel`, `reverse`, `payment`.

**التنفيذ:**

- صفحات الإنشاء والتعديل والتفاصيل في `features/purchases/pages`.
- Smart Delete يمنع حذف عملية لها أثر تشغيلي دون إظهار نتيجة آمنة.
- الاستلام هو النقطة التي يفترض أن تنشئ أو تحدث عناصر المخزون.
- تمت إضافة دعم barcode لبنود الشراء في migrations والتدفق الحالي.

**الخطر الأكبر المثبت بالاختبار:** اختبار `internal/purchases` يفشل حاليًا لأن fixture اختبار SQLite لا ينشئ `inventory_items`، بينما `ListSummaries` يستعلم منه. هذا ليس دليلًا وحده على فشل الإنتاج، لكنه دليل على أن اختبارات الوحدة لا تعكس schema runtime الكامل.

### 4.7 الموردون ودفتر المورد

**الوظيفة:** الموردون، الحد الائتماني، الرصيد، دفتر الحساب، المدفوعات، المشتريات، المخزون المرتبط، ومرتجعات المورد.

**التنفيذ:**

- `suppliers` لها repository/service/handler وواجهات ledger.
- توجد اختبارات supplier ledger lifecycle.
- يوجد فصل بين payments العامة وsupplier payments والـ ledger entries.

**مخاطر الاتساق:** تعدد جداول الدفتر التاريخية (`supplier_ledger`, `ledger_entries`, `supplier_payments`) يزيد احتمال أن تعرض شاشتان رصيدين مختلفين إن لم يكن مصدر الحقيقة موحدًا في كل عملية شراء/دفع/إرجاع.

### 4.8 العملاء والديون والدفعات

**الوظيفة:** العملاء، الحد الائتماني، كشف الحساب، المبيعات والدفعات والديون، الرصيد الحالي، المتأخرات، والإيصالات.

**التنفيذ:**

- العميل يملك ledger وfinancial timeline وdebt summary.
- حالات الدين المفتوحة موحدة جزئيًا في `business/debt_rules.go`.
- Worker يحول/يفحص الديون الدورية.
- dashboard يحسب المتأخر اعتمادًا على الرصيد وتاريخ الاستحقاق.
- توجد اختبارات للـ debt handler وmetrics.

**نقطة مالية:** يجب الحفاظ على اختبار يثبت عدم احتساب الضريبة مرتين عند خصم مرتجعات العملاء من صافي الإيراد، وهي معالجة موثقة في [memories/repo/profit-tax-fix.md] ولا ينبغي فقدها عند إعادة بناء التقارير.

### 4.9 المصروفات

**الوظيفة:** إنشاء وتعديل وحذف واعتماد ورفض المصروفات، التصنيفات، والتلخيص.

**التنفيذ:**

- Backend يوفر حالات approval، recurring expenses، وsummary.
- Worker يشغل `EnsureRecurringExpenses` كل ساعة.
- الواجهة لها اختبار صفحة مصروفات.
- التقرير يدعم مصروفات حسب التصنيف مع fallback لبنية response متعددة.

**ملاحظة:** recurring expense يعمل في Worker مستقل؛ إذا لم يعمل Worker أو اتصل بقاعدة مختلفة، سيظهر الفرق بين المصروفات المتوقعة والبيانات الفعلية.

### 4.10 المرتجعات ومرتجعات الموردين

**الوظيفة:** طلب المرتجع، البنود، الموافقة/الرفض، refund، الإكمال، العكس، التحقق من الكمية، إرجاع العميل، وإرجاع المورد.

**التنفيذ:**

- returns لها دورة حالة واسعة وواجهات list/create/details.
- supplier returns لها module منفصل مع lifecycle tests.
- `return_items` تربط sale item/inventory item عندما تتوفر الهوية.
- تم دعم حالات توافقية في SQLite حتى لا تفشل مزامنة بيانات تاريخية بسبب nullable columns.

**الخطر:** المرتجع ليس مجرد خصم مالي؛ يجب أن يغير حالة المخزون ويعيد أو يعزل العنصر حسب condition. اختبار دورة barcode الكاملة موجود في الشجرة الحالية، لكن يجب تشغيله ضمن بيئة API/DB حقيقية قبل اعتبار الدورة مكتملة.

### 4.11 القطع المستعملة والاستحواذ

**الوظيفة الحالية:** `usedparts` و`acquisitions` تدير إدخال القطع المستعملة، مخزونها، وربطها بالعميل أو المصدر.

**ملاحظة تاريخية مهمة:** جداول inspection وaging القديمة تُنشأ ثم تزال/تُنظف في `localdb` حسب حالة المخطط الحالية، وهناك migration تزيل بعض workflows المتقاعدة. لذلك لا ينبغي اعتبار كل مجلد تاريخي أو كل جدول قديم وظيفة مدعومة حاليًا.

### 4.12 الباركود

**الوظيفة:** توليد/حفظ/حل barcode، البحث في المنتج وعنصر المخزون، وربط الشراء والبيع والمرتجع.

**التنفيذ:**

- وحدة `internal/barcodes` تقدم resolver مركزيًا.
- الواجهة تدعم الإدخال والبحث والمسح عبر POS والمخزون.
- توجد اختبارات SQLite واختبار E2E جديد في [frontend/e2e/barcode-lifecycle.spec.ts](frontend/e2e/barcode-lifecycle.spec.ts).
- قارئ USB HID موثق في [docs/BARCODE-HID-SETUP-AND-TEST-GUIDE.md](docs/BARCODE-HID-SETUP-AND-TEST-GUIDE.md).

**معيار النجاح:** نفس barcode يجب أن يحتفظ بهويته عبر Product -> Purchase -> Receive -> Inventory -> Sale -> Return، وألا يؤدي البحث إلى إنشاء عنصر جديد.

### 4.13 التقارير والتحليلات

**الوظيفة:** تقارير المبيعات الصافية، الضريبة، الربح، المشتريات، المصروفات، المرتجعات، المخزون، القطع المستعملة، الديون والموردين، مع التصدير والطباعة.

**التنفيذ:**

- ReportsPage تعتمد على hook موحد لاختيار نوع التقرير والفترة.
- توجد معالجة متعددة لأشكال payload، وCSV/print.
- توجد حسابات يومية وشهرية في dashboard/aggregations.
- ملفات aggregation موجودة، لكن يجب التحقق دائمًا من أن كل endpoint يقرأ summary tables أو الاستعلامات الحقيقية، لا بيانات تجريبية قديمة.

**الخطر:** التقرير المالي يجب أن يحدد بوضوح الفرق بين gross revenue وnet revenue وtax وrefunds وcost، وأن يربط كل رقم بمصدر واحد قابل للتدقيق.

### 4.14 الإعدادات والتهيئة الإقليمية

**الوظيفة:** اسم المتجر، الضريبة، العملة، الخصومات، المنطقة الزمنية، المظهر، إعدادات قاعدة البيانات، المزامنة، وحذف البيانات الإداري.

**التنفيذ:**

- إعدادات عامة وإقليمية لها Backend وواجهة.
- المنطقة الزمنية الافتراضية `Asia/Jerusalem` والعملة `ILS`.
- DatabaseSettings تعرض أن SQLite هي قاعدة العمليات، وتتيح التحقق من الاشتراك والمزامنة اليدوية.
- التحويل الصريح online/offline غير موجود كمسار API حالي؛ هذا متعمد بحسب سياسة الاشتراك الحالية.

**تحذير تشغيلي:** أزرار حذف البيانات المحلية/السحابية destructive ويجب أن تبقى خلف Admin، مع backup وaudit واضحين قبل التشغيل في متجر حقيقي.

### 4.15 الإشعارات والتدقيق

**الوظيفة:** إشعارات المخزون المنخفض والديون والأحداث، تفضيلات الإشعار، وسجل التدقيق.

**التنفيذ:**

- `notifications` لها CRUD وunread count وmark read/preferences.
- Worker ينشئ إشعارات حسب المهام الدورية.
- audit routes/repository تسجل العمليات الإدارية والتشغيلية حيث يتم استدعاؤها.

**نقطة تحقق:** يجب التأكد أن كل mutation المالي يسجل actor وrequest ID والكيان القديم والجديد، لا أن يعتمد ذلك على بعض handlers فقط.

### 4.16 المستخدمون والصلاحيات

**الوظيفة:** إدارة المستخدمين وفحص admin.

**التنفيذ:**

- `middleware.Admin()` يحمي المسارات الإدارية.
- admin emails تضبط خارجيًا عبر `PARTFLOW_ADMIN_EMAILS` مع owner افتراضي للتوافق.
- مزامنة snapshot وpush وحذف قاعدة البيانات والتحقق من الاشتراك مقيدة إداريًا.

**الخطر:** السحابة الحالية موصوفة في الكود بأنها single-tenant. هذا يجعل عزل البيانات بين متاجر متعددة غير مكتمل ما لم تكن كل قاعدة/خدمة مخصصة لمتجر واحد أو تتم إضافة tenant isolation حقيقي.

## 5. المزامنة وOffline

### ما هو مطبق فعليًا

- `sync_queue` محلية مع `idempotency_key` ومحاولات و`last_error` و`next_retry_at` و` synced_at`.
- `sync_conflicts` محلية لتسجيل التعارضات.
- endpoint snapshot أولي في `/sync/initial-data`.
- endpoint queue push في `/sync/push`.
- إعدادات `/settings/sync` للمزامنة اليدوية والتنزيل والرفع وقراءة/حل التعارضات.
- CloudGuard يمنع الطلبات المحمية بلا cloud token صالح.

### ما لا ينبغي افتراضه

- لا يوجد fallback تلقائي كامل من السحابة إلى SQLite لكل طلب.
- لا يوجد online/offline switch عام في router الحالي.
- المزامنة ليست background automatic بالكامل؛ الواجهة تعرض أفعالًا يدوية.
- لا يوجد عزل tenant فعلي في snapshot operational tables؛ حماية المسار Admin فقط ليست بديلًا عن تصميم multi-tenant.

### التقييم

التصميم مناسب لسياسة “السحابة تقرر الاشتراك، وSQLite تحفظ التشغيل”، لكنه ليس Offline-first حرًا بالكامل لأن تنفيذ mutation يتطلب تحققًا سحابيًا. يجب تسمية هذا الوضع بدقة في المنتج حتى لا يتوقع المستخدم استمرار البيع أثناء انقطاع الإنترنت.

## 6. Worker والمهام الدورية

Worker مستقل في [backend/cmd/worker/main.go](backend/cmd/worker/main.go) يشغل:

- انتهاء الحجوزات كل 5 دقائق.
- فحص الديون كل ساعة.
- فحص المخزون المنخفض كل 30 دقيقة.
- ملخصات يومية كل 24 ساعة.
- توليد المصروفات المتكررة كل ساعة.

يعمل Worker على نفس قاعدة Backend ويجب أن يملك `DATABASE_URL` المطابق. وجود Worker كخدمة منفصلة جيد لعزل الأعمال الدورية عن latency الـ API، لكن فشل Worker قد لا يظهر للمستخدم مباشرة. يلزم health/heartbeat أو metric واضح لكل مهمة، مع منع التكرار إذا شغلت نسختان من Worker.

## 7. قاعدة البيانات والمigrations

يوجد عدد كبير من migrations المتراكمة حتى `066`. هذا يعكس تاريخ تطوير نشط، لكنه يخلق مخاطر:

- أرقام migrations متكررة مثل `027` و`031` و`032` و`033` و`034` و`035`.
- اختلاف بين schema PostgreSQL وschema SQLite المنشأ برمجيًا.
- طبقة compatibility schema تنشئ جداول قديمة وحديثة معًا.
- SQLite لا يدعم بعض تغييرات الأعمدة مباشرة، لذلك توجد عمليات table rebuild معقدة.
- بعض الجداول والحقول متاحة في localdb وليست مضمونة في كل migration سحابية.

المبدأ الصحيح مستقبليًا هو وجود schema contract واحد لكل release، واختبار bootstrap من قاعدة فارغة، واختبار upgrade من نسخة قديمة، واختبار snapshot import على SQLite جديدة.

## 8. الأمن

### نقاط جيدة

- منع secret الافتراضي و`DISABLE_AUTH` في release.
- CloudGuard في الوضع المحلي.
- refresh token digest بدل تخزين القيمة الخام.
- Admin middleware لمسارات الإدارة والمزامنة.
- تحديد حجم ونوع صور Electron والتحقق من identifiers.
- CORS يعكس origins المسموحة بدل السماح العشوائي عندما تكون القائمة مضبوطة.
- logging وrequest ID وsecurity logging موجودة.

### نقاط تحتاج مراجعة

1. `CORS_ALLOWED_ORIGINS=*` كقيمة افتراضية يحتاج منعًا أو تحذيرًا صريحًا في الإنتاج.
2. cache تحقق cloud لمدة 15 ثانية يوازن الأداء والأمان، لكنه يعني أن الإلغاء ليس لحظيًا.
3. snapshot السحابي يقرأ جداول تشغيلية كاملة؛ حماية Admin ضرورية لكن العزل البنيوي أفضل.
4. يجب التأكد أن السجلات لا تحتوي tokens أو كلمات مرور أو بيانات مالية حساسة.
5. يجب تشغيل Electron مع `contextIsolation` وبدون `nodeIntegration` والتحقق من ذلك في إعداد BrowserWindow الكامل.

## 9. الاختبارات ونتائج التحقق

### Frontend

- `npm run build:check`: **نجح**.
- `npm run test:run`: **نجح: 15 ملفًا، 48 اختبارًا**.
- يوجد عدد محدود من اختبارات الوحدات مقارنة بعدد الصفحات والمسارات.
- توجد اختبارات E2E للباركود والمخزون الافتتاحي، لكن تغطية كل التدفقات المالية ليست مكافئة لعدد الوحدات.

### Backend

- `go test ./...`: **فشل في حزمة واحدة**.
- الاختبار الفاشل: `internal/purchases/TestListSummariesHandlesNullPurchaseDateOnSQLite`.
- سبب الفشل الظاهر: `no such table: inventory_items` داخل fixture SQLite.
- بقية الحزم الظاهرة في التشغيل تمر، ومنها auth وinventory وsales وreturns وsync وsettings.

### دلالة النتيجة

الفشل الحالي ليس سببًا لتغيير منطق الإنتاج مباشرة. الإصلاح الصحيح هو جعل fixture الاختبار ينشئ schema الحد الأدنى الذي يعتمد عليه الاستعلام، ثم إضافة اختبار bootstrap يستخدم schema المحلي الحقيقي حتى لا يتكرر الانفصال.

## 10. أهم المخاطر مرتبة

### حرجة

1. **العزل السحابي:** operational snapshot يعمل على schema موصوف بأنه single-tenant. لا يصلح هذا كمنصة متعددة المتاجر دون عزل أقوى.
2. **الدقة المالية:** تعدد مصادر ledger والتقارير يجعل أي اختلاف في tax/refund/cost خطرًا تجاريًا.
3. **اعتماد العمليات على CloudGuard:** انقطاع الإنترنت قد يمنع البيع رغم وجود SQLite محلية، وهو سلوك يجب أن يكون مقصودًا ومعلنًا.

### عالية

4. **فشل اختبار Backend:** fixture المشتريات غير متوافق مع الجداول المطلوبة.
5. **انحراف schemas:** migrations كثيرة وcompatibility schema ضخمة.
6. **Worker بلا مراقبة كافية:** فشل المهام الدورية قد يبقى صامتًا.
7. **حذف البيانات:** العمليات destructive تحتاج backup وإثبات audit قبل التنفيذ.

### متوسطة

8. **الاعتماد على response shape مرن في الواجهة:** يزيد تحمل التوافق لكنه يخفي أخطاء API.
9. **تعدد أنواع الكيان:** Product وInventory Item وAcquisition Item تحتاج عقود معرفات واضحة.
10. **تغطية E2E محدودة:** المسار الكامل للشراء والاستلام والبيع والمرتجع والدفع يحتاج اختبارًا واحدًا متكاملًا.

## 11. خطة الأولويات المقترحة

### المرحلة الأولى: تثبيت الحالة

1. إصلاح fixture اختبار `purchases` وإعادة `go test ./...` إلى حالة خضراء.
2. تشغيل اختبار migration من قاعدة PostgreSQL فارغة ومن SQLite فارغة.
3. تجميد تقرير schema فعلي يبين الجداول والحقول المستخدمة لكل module.
4. مراجعة أي فرق بين التقرير القديم [CODEBASE_ANALYSIS_REPORT.md](CODEBASE_ANALYSIS_REPORT.md) والكود الحالي وإضافة تاريخ/حالة لكل finding.

### المرحلة الثانية: ضمان الأموال والمخزون

1. تعريف مصدر واحد للـ ledger لكل عميل ومورد.
2. تثبيت اختبارات tax، discount، refund، cost، profit، cash change، والدفعات الجزئية.
3. بناء E2E واحدة تمر عبر Product -> Purchase -> Receive -> Inventory -> POS -> Payment -> Return.
4. جعل barcode و`inventory_item_id` العقد الموحد في كل مرحلة.

### المرحلة الثالثة: المزامنة والعزل

1. تقرير واضح هل النظام single-store أم multi-tenant.
2. إن كان multi-tenant: إضافة tenant boundary إلى كل جدول تشغيلي وكل query وكل snapshot.
3. جعل sync operations idempotent مع conflict policy محددة لكل entity.
4. إضافة retry/backoff وheartbeat وmetrics للـ Worker والمزامنة.

### المرحلة الرابعة: التشغيل الإنتاجي

1. منع CORS wildcard في release.
2. إضافة backup قبل destructive reset وrestore drill دوري.
3. إضافة health check يثبت اتصال Worker ونسخة schema.
4. إضافة مراقبة للأخطاء المالية ورفض mutation عند schema mismatch بدل تسجيل نجاح وهمي.

## 12. الخلاصة النهائية

PartFlow يملك أساسًا عمليًا قويًا لنظام متجر محلي مع مزامنة سحابية: الواجهة واسعة، API منظم، SQLite/Electron موجودان، والمصادقة السحابية مفصولة عن التشغيل المحلي. البناء الحالي ينجح في الواجهة، والاختبارات الأمامية تمر، كما أن معظم اختبارات Backend تمر.

لكن جاهزية النظام لا تعتمد على عدد الشاشات فقط. المخاطر الحقيقية الآن هي حدود العزل السحابي، اتساق الحسابات المالية، انحراف schema بين PostgreSQL وSQLite، وسلوك “Offline” الذي ما زال يتطلب تحققًا سحابيًا قبل mutation. أول إجراء تقني منخفض المخاطر هو إصلاح اختبار SQLite الفاشل وتوحيد schema fixtures، ثم إثبات دورة مالية كاملة باختبار E2E واحد، وبعد ذلك اتخاذ قرار صريح بشأن single-tenant أو multi-tenant قبل توسيع النشر.
