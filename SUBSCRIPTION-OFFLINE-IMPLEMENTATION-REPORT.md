# تقرير تطبيق سياسة الاشتراك والبيانات المحلية

**التاريخ:** 2026-09-15
**النظام:** PartFlow
**الوثيقة المرجعية:** `SUBSCRIPTION-OFFLINE-STRATEGY.md`

> **تحديث السياسة - 2026-09-18:** تم اعتماد التشغيل Online-only. هذا التحديث يتقدم على أي وصف سابق للعمل التشغيلي دون اتصال في هذه الوثيقة.

## الملخص

تم تثبيت مبدأ الفصل بين صلاحية الحساب ومكان تخزين بيانات المتجر:

- قرار الاشتراك والمصادقة يبقى معتمدًا على الخادم السحابي.
- يمكن أن تستخدم بيانات التشغيل SQLite محليًا في وضع `local`، لكن ذلك لا يعني السماح بالعمل دون اتصال.
- لا يعود الوضع المحلي تلقائيًا إلى PostgreSQL السحابية.
- المزامنة بين السحابة وSQLite تبقى يدوية ومحمية.
- لا تُحذف بيانات SQLite عند انتهاء الاشتراك أو انقطاع الاتصال، لكن لا يُسمح باستخدام البرنامج دون تحقق سحابي.

## تحديث Online-only المنفذ في 2026-09-18

تم تغيير سياسة التشغيل من السماح باستمرار الجلسة المحلية أثناء انقطاع الإنترنت إلى اشتراط اتصال فعلي بالخادم السحابي:

- عند فقدان الإنترنت، تنهي الواجهة الجلسة الحالية وتوجه المستخدم إلى شاشة تسجيل الدخول.
- عند فتح البرنامج دون اتصال، لا يستعيد `checkAuth` جلسة محفوظة محليًا.
- لا يكفي وجود JWT محلي أو بيانات SQLite للسماح بالدخول.
- يبقى `CloudGuard` في الـ Backend هو الحاجز النهائي، ويتطلب `X-PartFlow-Cloud-Token` صالحًا قبل تمرير الطلبات المحمية.
- انتهاء الاشتراك أو تعطيل الحساب يمنع الطلبات من الخادم، حتى لو بقيت بيانات المتجر محليًا.
- لا توجد فترة سماح Offline في السياسة الحالية، ولا يسمح التطبيق بالبيع أو المشتريات أو تعديل المخزون دون تحقق سحابي.

### سياسة الخروج التلقائي بعد التحديث

تم تضييق أسباب الخروج التلقائي لمنع تكرار تسجيل الخروج بسبب أعطال مؤقتة:

- `401` منفرد أو فشل تجديد التوكن لا يمسح الجلسة تلقائيًا.
- أخطاء `500` و`503` ومشاكل الخادم السحابي المؤقتة لا تعتبر انتهاء اشتراك.
- فشل عملية تجارية بسبب تعذر التحقق لا يستدعي `logout()`؛ تفشل العملية فقط وتبقى الجلسة حتى ينجح تحقق لاحق.
- الخروج التلقائي يبقى محصورًا في فقدان اتصال الإنترنت أو رد `403` صريح من تحقق الاشتراك يفيد بأن الحساب غير نشط أو الاشتراك منتهٍ.
- عند عودة الاتصال، يعاد التحقق من السحابة قبل السماح بالعمليات المحمية.

### الملفات التي نُفذت عليها السياسة

- `frontend/src/App.tsx`
	- ربط حدث فقدان الاتصال بإنهاء الجلسة والعودة إلى تسجيل الدخول.
- `frontend/src/stores/authStore.ts`
	- منع استعادة الجلسة المحفوظة عندما يكون الجهاز Offline.
	- تصدير مسار إنهاء الجلسة الموحد لاستخدامه عند فقدان الاتصال.
- `backend/internal/auth/cloud_guard.go`
	- الحارس السحابي الموجود يرفض الطلبات المحمية عند غياب أو فشل تحقق توكن السحابة.

### التحقق من التنفيذ

تم تنفيذ:

```text
npm run lint:types
npm run build
```

ونجح فحص TypeScript وبناء الإنتاج. تعذر على أداة تشغيل الاختبارات التعرف على ملف اختبار المصادقة الحالي، لذلك لم تُسجل اختبارات وحدة إضافية كاختبارات ناجحة. لم تتم إضافة فترة سماح أو تخزين رخصة Offline؛ لذلك فإن انقطاع الإنترنت يمنع الاستخدام بدل السماح باستمراره.

### حدود الحماية

هذه السياسة تمنع الاستخدام الطبيعي بعد انتهاء الاشتراك لأن القرار النهائي يصدر من الخادم. لكنها لا تمنع مستخدمًا يملك صلاحية Administrator من تعديل ملفات البرنامج محليًا بشكل مطلق. الحماية الفعلية هي أن الخادم لا يقبل الطلبات أو المزامنة دون جلسة وتحقق اشتراك صالحين.

## التغييرات المنفذة

### 1. منع التحويل الصامت إلى السحابة

في `backend/pkg/config/config.go` تم إصلاح `ResolveDatabaseURL`:

- كان `DB_CONNECTION_MODE=local` يستخدم `DATABASE_URL` السحابي كحل احتياطي.
- أصبح الوضع المحلي يستخدم SQLite الافتراضية دائمًا عند عدم وجود `DATABASE_URL_LOCAL`.
- بقي اختيار PostgreSQL متاحًا فقط عند تحديد وضع `cloud` صراحةً أو من خلال إعداد بيئة النشر.

تم تحديث `backend/.env` ليكون وضع التطوير الافتراضي:

```text
DB_CONNECTION_MODE=local
```

### 2. اختبار عدم الرجوع إلى PostgreSQL

تم تعديل اختبار الإعدادات في `backend/pkg/config/config_test.go` ليضع رابط PostgreSQL أثناء اختبار الوضع المحلي، ويتأكد من استمرار اختيار SQLite.

### 3. التحقق السحابي والعمليات

تمت مراجعة المسار الحالي والتأكد من أن:

- تسجيل الدخول يتحقق من الخادم السحابي أولًا.
- الـ Backend المحلي يطلب `X-PartFlow-Cloud-Token` للتحقق من الاشتراك.
- فشل التحقق أو انتهاء الاشتراك يمنع الطلبات المحمية.
- المزامنة السحابية ورفع العمليات المحلية محميان بصلاحيات المدير.
- المزامنة لا ترفع نسخة SQLite كاملة، بل تعالج العمليات الموجودة في `sync_queue` فقط.

### 4. تحسين رسائل تسجيل الدخول

تم تعديل الواجهة لتمييز فشل الاتصال عن بيانات الدخول الخاطئة:

- انقطاع الخادم أو الشبكة يعرض رسالة مناسبة عن تعذر الاتصال.
- خطأ `401` فقط يعرض رسالة البريد الإلكتروني أو كلمة المرور غير صحيحة.
- أضيفت رسائل عربية وإنجليزية لهذا النوع من الأخطاء.
- رسالة خطأ تسجيل الدخول لا تبقى بعد تحديث صفحة الدخول.

الملفات ذات العلاقة:

- `frontend/src/features/auth/pages/LoginPage.tsx`
- `frontend/src/stores/authStore.ts`
- `frontend/src/lib/error-messages.ts`
- `frontend/src/i18n/ar.ts`
- `frontend/src/i18n/locales/ar.json`
- `frontend/src/i18n/locales/en.json`

## حالة البيانات

تم التحقق من أن البيانات السحابية لم تُحذف. كانت المشكلة أن التشغيل بوضع `local` يقرأ ملف SQLite المحلي، بينما كان هذا الملف غير مُزامَن ويحتوي على بيانات تشغيل قليلة/فارغة.

مسار SQLite المحلي:

```text
%APPDATA%\\PartFlow\\data\\partflow.db
```

وفق السياسة، تنزيل نسخة البيانات من السحابة إلى SQLite يجب أن يتم عبر المزامنة اليدوية المحمية بحساب المدير. لم يتم فتح المزامنة للمشترك العادي لأن مخطط السحابة الحالي يحتوي جداول تشغيل غير معزولة بالكامل حسب المستخدم.

## التحقق والتنفيذ

تم تنفيذ الاختبارات التالية بنجاح:

```text
go test ./pkg/config ./pkg/middleware ./internal/sync
go test ./...
npx tsc --noEmit
npm run build
```

كما تم التحقق من:

- تشغيل الـ API على SQLite بوضع `local`.
- استجابة `GET /health` بحالة `ok` وقاعدة بيانات `healthy`.
- عدم رجوع الوضع المحلي إلى PostgreSQL عند وجود `DATABASE_URL` السحابي.

## قرار التشغيل النهائي

التشغيل المطابق للوثيقة هو:

```powershell
cd C:\Users\Administrator\Desktop\PartFlow\backend
$env:SERVER_MODE="debug"
$env:DB_CONNECTION_MODE="local"
go run .\cmd\api
```

أما PostgreSQL السحابية فتظل مصدر المصادقة والتحقق من الاشتراك، وليست قاعدة التشغيل المحلية الافتراضية.

## إصلاح فشل المزامنة

ظهرت رسالة:

```text
فشل حفظ بيانات السحابة محلياً
```

والتفصيل التقني كان:

```text
seed sale_items: constraint failed: FOREIGN KEY constraint failed
```

كان جدول `sale_items` يُستورد قبل جدول `inventory_items`، رغم أن بعض عناصر البيع تحتوي مرجعًا إلى عنصر مخزون. تم تعديل ترتيب الاستيراد في `backend/internal/localdb/localdb.go` ليصبح:

1. المنتجات والموردون.
2. عناصر المخزون.
3. المبيعات.
4. عناصر المبيعات.

المزامنة تعمل داخل معاملة SQLite ذرية، لذلك لا تُحفظ نسخة جزئية عند حدوث فشل.

تمت إعادة المزامنة بنجاح، وكانت النتائج:

- المنتجات: 30
- عناصر المخزون: 752
- المشتريات: 4
- المبيعات: 5
- العملاء: 2
- الموردون: 2

بعد إعادة تشغيل الـ Backend لتحديث cache لوحة التحكم، ظهرت الإحصاءات المحلية بشكل صحيح: 30 منتجًا، 746 عنصرًا متاحًا ضمن توزيع المخزون، وإجمالي مشتريات ₪7,080.
