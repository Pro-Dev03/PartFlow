# تقرير إصلاح سياسة الاشتراك والأوفلاين

**التاريخ:** 2026-09-02  
**المشروع:** PartFlow  
**المرجع:** [SUBSCRIPTION-OFFLINE-STRATEGY.md](SUBSCRIPTION-OFFLINE-STRATEGY.md)  
**Commit الإصلاح المنشور:** `f45bf79`

## الملخص التنفيذي

تم إصلاح مجموعة من مشاكل المصادقة، تجديد الجلسات، توافق PostgreSQL وSQLite، وطلبات لوحة التحكم. تم اختبار تسجيل الدخول واللوحة بشكل حي، وأصبحت طلبات الإحصاءات اليومية والتنبيهات تعمل بنجاح.

مع ذلك، لا يمكن اعتبار التطبيق متوافقًا مع السياسة بنسبة 100% حتى يتم إغلاق الفجوات المذكورة في هذا التقرير، وأهمها استمرار الجلسة عند انقطاع الإنترنت ووجود مفاتيح تجاوز للمصادقة في كود التشغيل.

## الإصلاحات المنفذة

### المصادقة والجلسات

- تسجيل الدخول يمر عبر خدمة السحابة.
- إنشاء جلسة محلية يتم بعد نجاح تسجيل الدخول السحابي.
- توحيد مفاتيح access token وrefresh token.
- حفظ وتدوير refresh token.
- منع حلقات refresh المتكررة عند فشل التجديد.
- تنظيف بيانات الجلسة عند تسجيل الخروج أو رفض التوكن.
- توجيه أخطاء انتهاء الاشتراك إلى صفحة انتهاء الاشتراك.
- إبقاء أخطاء الصلاحيات العادية منفصلة عن انتهاء الاشتراك.

### التحقق من الاشتراك

- عمليات التعديل في الواجهة تتحقق من السحابة قبل الإرسال.
- Backend المحلي يمرر توكن السحابة للتحقق عند تفعيل `PARTFLOW_REQUIRE_CLOUD_AUTH`.
- تعطيل الحساب أو انتهاء الاشتراك يعاد كرفض واضح بدل الاعتماد على بيانات SQLite المحلية.
- المزامنة تستخدم اتجاه السحابة إلى SQLite المحلية.

### توافق قواعد البيانات

- إصلاح استعلامات PostgreSQL التي كانت تفشل مع مخطط الملخصات legacy.
- دعم أعمدة `summary_date` و`summary_month` إلى جانب الأعمدة الحديثة.
- تحويل `total_sales` الرقمي إلى `int` عند القراءة من PostgreSQL.
- دعم سائق `pgx` في فحص الجداول والأعمدة.
- حماية تحديث ملخصات SQLite عند غياب جدول `inventory_movements`.
- تحسين اكتشاف أعمدة المرتجعات الاختيارية قبل تشغيل استعلام الربح.

### التنبيهات ولوحة التحكم

- إصلاح استعلام عدد التنبيهات غير المقروءة ليتوافق مع SQLite وPostgreSQL.
- إصلاح خطأ `daily-sales` الذي كان يعيد HTTP 500.
- منع فشل تحميل اللوحة بسبب اختلاف أنواع وأسماء أعمدة الملخصات.

## الاختبارات المنفذة

### اختبارات آلية

- `go test ./internal/api ./pkg/database` نجح.
- اختبارات مصادقة الواجهة وتجديد التوكن نجحت:
  - ملفا الاختبار: `authStore.test.ts` و`api-client-auth-refresh.test.ts`
  - النتيجة: **14 اختبارًا ناجحًا**.

### اختبار حي

تم تشغيل التطبيق واختبار المسار التالي:

1. فتح صفحة تسجيل الدخول.
2. تسجيل الدخول بحساب الاختبار.
3. الانتقال إلى لوحة التحكم.
4. إعادة تحميل الصفحة مع بقاء الجلسة.
5. فحص طلبات API الأساسية.

النتيجة الأخيرة:

| الطلب | النتيجة |
|---|---:|
| `GET /api/v1/aggregations/daily-sales` | 200 |
| `GET /api/v1/dashboard/stats` | 200 |
| `GET /api/v1/notifications` | 200 |
| ظهور رسالة خطأ في اللوحة | لا |

## الفجوات المتبقية

### 1. انقطاع الإنترنت لا يغلق الجلسة فعليًا

في `frontend/src/stores/authStore.ts`، مسار `checkAuth` يضبط الجلسة كمصادق عليها عند `navigator.onLine === false`. كما أن فشل الشبكة أثناء التحقق السحابي قد يعيد نجاحًا للحفاظ على الجلسة.

هذا يخالف نص السياسة، الذي يشترط وجود الإنترنت والتحقق السحابي للتشغيل والعمليات.

**الإجراء المطلوب:** عند فقدان الإنترنت يجب ضبط:

- `isAuthenticated = false`
- `sessionVerified = false`
- إيقاف العمليات الجديدة.
- توجيه المستخدم إلى صفحة الدخول أو صفحة اتصال مطلوبة.
- عدم حذف بيانات المتجر المحلية.

### 2. مفاتيح تجاوز المصادقة ما زالت موجودة

الكود يدعم:

- `PARTFLOW_REQUIRE_CLOUD_AUTH=false`
- `disableAuth`

هذه مفيدة للتطوير، لكنها تمنع ضمان الالتزام الكامل إذا وصلت إلى بيئة الإنتاج.

**الإجراء المطلوب:** منع هذه الخيارات في build الإنتاج أو جعل تشغيلها مشروطًا صراحةً ببيئة development، مع فشل startup إذا حاولت بيئة الإنتاج تعطيل التحقق.

### 3. Endpoint تغيير وضع التشغيل ما زال موجودًا

ما زال endpoint التالي مسجلًا:

`PUT /api/v1/settings/operating-mode`

ويقبل `online` و`offline`. الواجهة لا تعرض التبديل للمستخدم العادي، لكن endpoint نفسه يمثل بقاء مفهوم وضع offline في المسار التشغيلي.

**الإجراء المطلوب:** إزالة endpoint من تطبيق المشترك أو قصره على أدوات الإدارة والتطوير، مع عدم السماح له بتجاوز التحقق السحابي.

### 4. توقيع Electron غير مثبت بالكامل

وجود `signAndEditExecutable: true` لا يثبت أن ملفات الإنتاج موقعة بشهادة موثوقة.

**الإجراء المطلوب:** التحقق من إعداد شهادة التوقيع في CI أو Render/Windows build pipeline، وتوثيق نجاح التوقيع في artifact النهائي.

## التقييم النهائي

| المجال | الحالة |
|---|---|
| تسجيل الدخول السحابي | مطبق ومختبر |
| تدوير الجلسة | مطبق ومختبر |
| حماية عمليات التعديل | مطبقة في الواجهة، وتحتاج منع مفاتيح التجاوز في الإنتاج |
| مصدر قرار الاشتراك | السحابة هي المصدر المعتمد |
| تخزين بيانات المتجر | SQLite محلية |
| اتجاه المزامنة | السحابة إلى SQLite |
| حفظ البيانات عند انتهاء الاشتراك | مطبق |
| إغلاق الجلسة عند انقطاع الإنترنت | غير مكتمل |
| إزالة تبديل Online/Offline بالكامل | غير مكتمل |
| توقيع تطبيق Electron | يحتاج إثباتًا من pipeline الإنتاج |

## الخلاصة

الإصلاحات المنشورة حسنت الاستقرار وأزالت أخطاء التشغيل الرئيسية، خصوصًا خطأ `daily-sales` ومشاكل جلسات الدخول وتوافق PostgreSQL.

الحكم الحالي هو:

> **التطبيق متوافق بدرجة كبيرة مع السياسة، لكنه ليس متوافقًا معها 100% بعد.**

لا يصبح الحكم 100% إلا بعد تنفيذ إغلاق الجلسة عند فقدان الإنترنت، منع مفاتيح تجاوز المصادقة في الإنتاج، إزالة endpoint تغيير وضع التشغيل من مسار المشترك، وإثبات توقيع نسخة Electron النهائية.
