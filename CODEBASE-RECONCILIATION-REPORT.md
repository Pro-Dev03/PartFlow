# تقرير مطابقة وتنفيذ إصلاحات PartFlow

**التاريخ:** 31 أغسطس 2026
**المرجع:** `CODEBASE_ANALYSIS_REPORT.md`
**الحالة:** مكتمل برمجياً للمسارات التشغيلية الأساسية، مع تطبيق migration `054` على Supabase وبناء Electron محلياً؛ تم رفع المصدر إلى GitHub، ويتبقى تأكيد إعادة نشر الخدمات واختبار Render النهائي.

## 1. التصميم المعتمد

- بيانات البيع والمخزون والعملاء والديون والمشتريات والمصروفات تحفظ في SQLite داخل جهاز المستخدم.
- Render/Supabase هما مصدر صلاحية الحساب والاشتراك فقط، ولا يعتمد التطبيق على اتصال قاعدة البيانات السحابية لتنفيذ عملية المتجر اليومية.
- تسجيل الدخول والتحقق من الاشتراك يتطلبان اتصالاً بالإنترنت ونجاح التحقق السحابي.
- بعد نجاح الدخول لأول مرة، تُنزّل نسخة السحابة إلى SQLite تلقائياً مرة واحدة لكل حساب/جهاز.
- بعد ذلك تكون مزامنة البيانات **يدوية** من الإعدادات عبر «مزامنة سحابية الآن»، واتجاهها السحابة ← SQLite.
- لا يوجد عامل يرفع عمليات SQLite تلقائياً إلى السحابة. بقيت جداول/دوال قائمة الانتظار للتوافق مع الإصدارات القديمة فقط.
- عند حذف/تعطيل الحساب من لوحة التحكم، يفشل التحقق السحابي ويتوقف الوصول للمسارات المحمية. تبقى قاعدة SQLite للعرض/الاسترجاع ولا تسمح الواجهة بعمليات جديدة.

## 2. ما تم تنفيذه

### التجميعات ولوحة التحكم

- أزيلت القيم التجريبية الثابتة من التجميعات اليومية والشهرية للمبيعات والمخزون والديون والأرباح.
- أصبحت التجميعات تعاد حسابها من الجداول التشغيلية الفعلية، مع صياغة منفصلة متوافقة مع PostgreSQL وSQLite.
- أضيفت معالجة `POST /aggregations/update` وقراءة حالة آخر تحديث.
- أصبحت بيانات الرسوم، توزيع المخزون، النشاط الأخير، الديون، العملاء، الموردين والإشعارات تُقرأ من SQLite في المسار المحلي.

### المشتريات والدفاتر

- إنشاء الشراء وعناصره ودفتر المورد وسجل التدقيق يتم داخل transaction واحدة.
- يدعم الكود مخططي `type` و`transaction_type` القديم/الجديد.
- أضيف تحديث رصيد المورد وتسجيل الدفعات بصورة متوافقة مع SQLite وPostgreSQL.
- أضيفت migration `053_supplier_ledger_runtime.sql` لتوحيد الأعمدة الاختيارية.

### الاستحواذ، الاستبدال، والمخزون

- إنشاء الاستحواذ ينشئ `inventory_items` ويربطها بـ`acquisition_items` داخل transaction.
- تُحدّث كمية المنتج المجمعة في جدول `inventory`.
- الاستبدال (Trade-in) ينشئ العميل/المنتج عند إدخال اسم يدوي، ثم يحفظ العنصر وسجل `trade_ins` الحقيقيين.
- حركات البيع والحجز والتحرير تسجل الكميات السابقة واللاحقة الفعلية.
- تحديثات الفحص الذرية تغيّر حالة الاستحواذ وعنصر المخزون معاً.
- أضيفت اختبارات SQLite للاستحواذ، Trade-in، الحركات والفحص.

### العامل الخلفي

- العامل الحقيقي في `backend/cmd/worker` ينفذ انتهاء الحجوزات، فحص الديون، فحص المخزون المنخفض، والتنبيهات/الرؤى اليومية.
- تم إصلاح استعلامات PostgreSQL/SQLite وعدّ الكميات وحركة تحرير الحجز.
- تم تعديل `worker/Dockerfile` و`render.yaml` ليبنيا العامل الحقيقي من `backend/cmd/worker` بدلاً من العامل التجريبي القديم.
- ينفذ Worker فحص الحجوزات والديون مرة عند البدء ثم وفق الجداول الدورية، ولا يحتاج إلى `PORT` أو قاعدة بيانات مستقلة.
- يجب أن يمرر Render إلى Worker نفس `DATABASE_URL` و`JWT_SECRET` المستخدمين في Backend؛ التفاصيل موثقة في `WORKER-DEPLOYMENT.md`.
- في `render.yaml` أصبح `JWT_SECRET` يُسحب مباشرة من Backend عبر `fromService.envVarKey`، لتجنب إنشاء مفتاح مختلف بالخطأ.

### المزامنة من السحابة إلى المحلي

- `GET /sync/initial-data` يجمع الجداول التشغيلية من خادم السحابة، وهو محمي الآن بصلاحية المدير لأن المخطط الحالي أحادي المستأجر.
- `POST /settings/sync` يرسل Authorization إلى Render ثم يدمج snapshot في SQLite داخل transaction.
- الدمج Upsert ولا يحذف الصفوف المحلية غير الموجودة في snapshot.
- يدعم snapshot العملاء، الموردين، المنتجات، المخزون، المبيعات، المشتريات، الديون، المدفوعات، المصروفات، الدفاتر، الاستحواذ، المرتجعات، الفحص، الإشعارات والتقارير.
- أضيفت حماية من الحقول غير المعروفة، وتحويل أسماء/قيم PostgreSQL وJSON إلى صيغة SQLite.
- أضيفت مسارات قراءة وحل تعارضات المزامنة؛ سياسة التشغيل الحالية cloud-to-local لا تولد تعارضات رفع جديدة.
- أضيفت نافذة المزامنة الأولية فعلياً إلى `App.tsx` بعد التحقق من الجلسة.
- لا تُفتح المزامنة للمشتركين العاديين قبل إضافة عزل `tenant_id` أو فصل قاعدة لكل متجر؛ هذا يمنع تسريب snapshot العام.

### الأمان والاعتمادية

- التحقق السحابي يسبق الوصول للمسارات المحمية وعمليات التعديل.
- تم تقييد JWT إلى HS256 والتحقق من `ValidMethods`.
- أضيف Token Bucket محلي لتحديد المعدل مع `RATE_LIMIT_RPS` و`RATE_LIMIT_BURST`، ويُفعّل في خادم API.
- وُحّدت استجابات أخطاء المسارات الأساسية، وأضيف تحقق من التواريخ والمبالغ والحالات والمعرفات.
- أضيفت توافقات SQL للعمليات والتقارير على SQLite، مع migrations `051` و`052` و`053` و`054` لإبطال Refresh Tokens.

## 3. اختبارات التحقق المنفذة

| الفحص | النتيجة |
|---|---|
| `go test ./...` من `backend` | ناجح — جميع الحزم |
| `go vet ./...` من `backend` | ناجح |
| `npm run test:run` من `frontend` | ناجح — 14 ملفاً و47 اختباراً |
| `npm run build:check` من `frontend` | ناجح — TypeScript وVite وPWA |
| `git diff --check` | ناجح؛ التحذيرات الظاهرة تخص تحويل LF/CRLF فقط |

اختبارات Smoke: `/health` و`/ready` و`/alive` المحلية والسحابية تعيد `200`، ومسارات الحماية غير الموثقة تعيد `401`، وطلب Refresh فارغ يعيد `400` تحققًا متوقعًا.

## 4. ما يلزم خارج بيئة الكود

هذه إجراءات نشر لا يمكن تنفيذها من الاختبارات المحلية تلقائياً:

1. تأكيد تطابق سجل migrations في بيئة Render مع Supabase؛ migration `054` طُبقت على قاعدة Supabase الحالية.
2. إعادة نشر خدمة Backend وخدمة Worker في Render.
3. ضبط `PARTFLOW_REQUIRE_CLOUD_AUTH=true` و`PARTFLOW_CLOUD_API_URL=https://partflow-api.onrender.com/api/v1` في بيئة التشغيل المحلية، والتأكد من `DATABASE_URL` و`JWT_SECRET` في Render.
4. توزيع نسخة Electron المبنية أو إعادة بنائها بعد نشر الـBackend عند الحاجة.
5. اختبار تسجيل الدخول ثم «مزامنة سحابية الآن» من جهاز نظيف، والتأكد من ظهور العملاء والديون والمخزون والتقارير.

## 5. حدود الحماية

لا يمكن لأي برنامج مكتبي منع مستخدم يملك صلاحية كاملة على جهازه من تعديل ملف executable أو SQLite يدوياً. الحماية الفعلية هنا هي أن قرار السماح بالجلسة والاشتراك صادر من الخادم، وكل مسار محمي يرفض الجلسة عند تعطيل الحساب أو انتهاء التحقق. لذلك لا ينبغي ضبط `PARTFLOW_REQUIRE_CLOUD_AUTH=false` في الإنتاج.

## 6. تنبيه أمني عاجل

تمت مشاركة بيانات اتصال PostgreSQL الحساسة في المحادثة سابقاً. يجب تدوير كلمة مرور قاعدة البيانات/سلسلة الاتصال فوراً من Supabase وتحديث Render وملفات البيئة، وعدم وضعها في Git أو داخل Electron.

**الخلاصة:** الإصلاحات البرمجية الأساسية منفذة ومختبرة، وmigration `054` مطبقة على Supabase. المتبقي هو إعادة النشر والاختبار على بيئة Render الفعلية.
