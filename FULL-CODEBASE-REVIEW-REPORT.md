# تقرير المراجعة الشاملة لمشروع PartFlow

**التاريخ:** 31 أغسطس 2026  
**النطاق:** Backend، Frontend، SQLite/PostgreSQL، المصادقة، المزامنة، Electron، الاختبارات والنشر.

## النتيجة التنفيذية

البناء والاختبارات الأساسية ونقاط الصحة تعمل، كما أزيلت سابقاً البيانات الوهمية من التجميعات وأصلحت مسارات الاستحواذ والمشتريات والفحص والعامل الخلفي. لكن لا يُنصح بالنشر الإنتاجي قبل معالجة حواجز أمنية ووظيفية محددة.

## المشاكل الحرجة

1. **صلاحيات الإدارة غير محمية:** كانت جميع مسارات إدارة المستخدمين والاشتراكات، حذف قاعدة البيانات، وتطبيق الترحيلات تعتمد على تسجيل الدخول فقط. تم الآن فرض `Admin` مركزية على هذه المسارات، مع قائمة `PARTFLOW_ADMIN_EMAILS` ورفض المستخدم العادي.
2. **التسجيل العام يمنح اشتراكاً فعالاً:** كان مسار `/auth/register` يسمح بإنشاء حساب فعال لمدة سنة. أصبح التسجيل العام موقوفاً افتراضياً، ولا يُفتح إلا بتعيين `PARTFLOW_ALLOW_PUBLIC_REGISTRATION=true` صراحةً.
3. **عزل بيانات المشتركين:** المخطط السحابي الحالي أحادي المستأجر ولا يحتوي أعمدة حساب لمعظم جداول العمليات. تم تطبيق حماية فشل-آمن: snapshot السحابي والمزامنة لا يسمح بهما إلا للمشرف المصرح، إلى أن يتم تنفيذ `tenant_id` أو قاعدة منفصلة لكل متجر. لا ينبغي فتحهما للمشتركين العاديين قبل ذلك.
4. **تعارض التصميم السحابي/المحلي:** الخادم السحابي ما زال يخزن ويخدم معظم بيانات العمليات، بينما التصميم المعتمد يحدد السحابة للتحقق من الحساب والاشتراك فقط.
5. **أسرار مكشوفة:** بيانات اتصال PostgreSQL أُرسلت سابقاً في المحادثة؛ يجب تدوير كلمة المرور وتحديث Render. يجب أيضاً إبقاء المفتاح الخاص خارج Git ونسخة Electron.

## المشاكل عالية الأولوية

- مسار قارئ الباركود في الواجهة `/barcode/scan` غير موجود في الخادم (تم تصحيحه إلى `/barcodes/product/{barcode}`).
- مسارات تعديل/حذف عناصر المرتجعات في الواجهة لا تطابق مسارات الخادم (تم تصحيحها لتتضمن `return_id`).
- دوال إدارة المستخدمين القديمة في الواجهة تستخدم `/settings/users` بينما الخادم يستخدم `/users` (تم تصحيحها).
- تم تفعيل إبطال Refresh Token وتدويره عند التجديد، وطبقت migration `054_refresh_token_revocation.sql` على قاعدة Supabase.
- ذاكرة التخزين المؤقت في الواجهة غير مرتبطة بالمستخدم، وقد تعرض بيانات الحساب السابق عند تبديل الحساب (تم تنظيفها عند تسجيل الدخول وإلغاء التخزين المؤقت لخدمات API في PWA).
- جدول `sync_queue` ودوال رفع العمليات موجودة، لكن لا توجد نقاط إنشاء فعلية للعمليات في قائمة الانتظار؛ الرفع التلقائي غير مضمون.
- المزامنة السحابية تعمل Upsert ولا تحذف الصفوف المحلية المحذوفة من السحابة.
- تقرير صافي المبيعات لا يستبعد كل حالات البيع الملغاة/المعكوسة (تم استبعاد الحالات غير المكتملة في الاستعلامات الأساسية).
- بعض أخطاء التاريخ في التقارير تُهمل ويُستخدم نطاق افتراضي بدلاً من إرجاع 400 (تم توحيد التحقق وإرجاع `INVALID_DATE_RANGE`).
- مشغل الترحيلات لا يسجل تطبيق SQL والتغيير في معاملة ذرية، وهدف Makefile لا يشغل كامل الترحيلات (تم إصلاح المعاملة وهدف `go run ./cmd/migrate`؛ يدعم المشغل الآن ملفات SQL القديمة التي تحتوي `BEGIN/COMMIT`).

## المشاكل المتوسطة

- `AdvancedSearch` يستخدم `Input` دون استيراده؛ فلاتر النص/التاريخ/الأرقام ستفشل وقت التشغيل (تمت إضافة الاستيراد).
- CORS يعكس أي Origin بدلاً من قائمة مسموحة (تمت إضافة `CORS_ALLOWED_ORIGINS` ورفض preflight غير المسموح).
- محدد المعدل محلي لكل نسخة ولا يتشارك الحالة بين نسخ Render.
- العامل الخلفي ينفذ فحص الحجوزات والديون فور التشغيل ثم دورياً؛ أما تنبيهات المخزون والرؤى فتبقى دورية لتجنب تكرار الإشعارات عند كل إعادة تشغيل.
- نسخة Worker الجذر قديمة مقارنة بـ `backend/cmd/worker`.
- التوكنات المحلية محفوظة في SQLite دون تشفير نظام التشغيل.
- تكامل الدفع والإشعارات الحالي تجريبي، وSupabase Auth يحتوي دوال غير منفذة.
- ملف `.env.example` يجب أن يحتوي قيماً توضيحية فقط، لا أسراراً تبدو حقيقية؛ يلزم استبدال قيمة `JWT_SECRET` الحالية بقيمة نموذجية وتدوير أي مفاتيح سبق مشاركتها.

## ما تم التحقق منه

- `go test ./...` ناجح.
- `go vet ./...` ناجح.
- اختبارات الواجهة: 14 ملفاً و47 اختباراً ناجحاً.
- بناء TypeScript/Vite ناجح.
- اختبارات المتصفح السابقة: 8/8 ناجحة؛ التشغيل الكامل الحالي يحتاج `E2E_EMAIL/E2E_PASSWORD` ومتصفحات Playwright المثبتة، ولذلك لا يُعد فشله الحالي دليلاً على عطل تطبيق.
- `/health` و`/ready` و`/alive` المحلية والسحابية تعيد 200.

## ترتيب الإصلاح المعتمد

1. ~~فرض صلاحية مدير مركزية على مسارات الحسابات والاشتراكات وحذف البيانات والترحيلات، مع تعطيل التسجيل العام أو جعله بدعوة.~~ **منفذ برمجياً.**
2. ~~منع تسريب snapshot العام~~ **منفذ:** مسارات snapshot والمزامنة محمية للمشرف المصرح. يتبقى عزل حقيقي (`tenant_id` أو قاعدة لكل متجر) إذا كان سيتم السماح لكل مشترك بمزامنة مستقلة.
3. ~~إصلاح مسارات الباركود والمرتجعات وإزالة مسارات الواجهة القديمة.~~ **منفذ برمجياً.**
4. تنظيف ذاكرة التخزين المؤقت وإبطال Refresh Token عند تبديل/خروج الحساب **منفذ**؛ migration `054_refresh_token_revocation.sql` مطبقة على قاعدة Supabase.
5. سياسة المزامنة الحالية سحابة ← SQLite يدوياً. ما زال يلزم قرار عزل الحسابات ومعالجة حذف الصفوف قبل السماح بأكثر من متجر في snapshot سحابي واحد.

## حدود الحماية

لا يمكن حماية SQLite أو executable بشكل مطلق من مستخدم يملك صلاحية كاملة على جهازه. الحماية العملية هي أن يكون قرار السماح بالجلسة والاشتراك من الخادم، وأن تُرفض كل المسارات المحمية عند تعطيل الحساب.

## تحديث التنفيذ الحالي — 31 أغسطس 2026

تم تنفيذ الإصلاحات البرمجية التالية بعد إنشاء المراجعة:

- إضافة `Admin` middleware ومسار فحص الصلاحية، وحماية مسارات المستخدمين والاشتراكات وحذف قاعدة البيانات والترحيلات.
- تعطيل التسجيل العام افتراضياً، وإضافة إعدادات Render وCORS اللازمة.
- تصحيح مسارات الباركود والمرتجعات، وإخفاء تبويب قاعدة البيانات عن المشترك العادي.
- منع تخزين استجابات API المصادق عليها في Service Worker، ومسح cache عند تبديل الحساب.
- تصحيح حساب صافي المبيعات والتحقق الصارم من نطاقات تواريخ التقارير.
- جعل تطبيق كل migration وتسجيلها ذرياً مع دعم ملفات migrations القديمة التي تحتوي معاملات داخلية.
- توحيد تاريخ البيع في تجميعات SQLite مع دعم قواعد البيانات القديمة التي لا تحتوي `sale_date`؛ يستخدم `sale_date` عند توفره و`created_at` كبديل آمن.
- تفعيل تخزين بصمة Refresh Token وتدويره عند التجديد وحذفه عند تسجيل الخروج، مع migration `054_refresh_token_revocation.sql` وتغطية اختبارية؛ القواعد القديمة تستمر مؤقتاً بوضع التوافق.
- إصلاح زر «تحقق من الاشتراك» في الواجهة ليستخدم مسار تجديد الخادم السحابي عبر مخزن المصادقة، ويحفظ الرمز الجديد بعد التدوير بدلاً من ترك رمز قديم مُلغى.
- إضافة إبطال أفضل-جهد للجلسة السحابية عند تسجيل الخروج، مع استمرار الخروج المحلي فوراً عند عدم توفر الإنترنت.
- توثيق إعدادات `PARTFLOW_REQUIRE_CLOUD_AUTH` و`PARTFLOW_CLOUD_API_URL` في `.env.example` لتثبيت سياسة التحقق السحابي في نسخة Electron/local.
- توثيق إعداد Worker في `WORKER-DEPLOYMENT.md`: يبنى من `backend/cmd/worker` ويشارك Backend نفس `DATABASE_URL` و`JWT_SECRET`، ولا يحتاج منفذاً أو قاعدة بيانات مستقلة.
- تصحيح تعريف خدمة Redis الاختيارية في `render.yaml` إلى Render Key Value (`keyvalue`) ومطابقة مراجع `fromService`.
- ربط `JWT_SECRET` في Worker مباشرة بمتغير Backend عبر `fromService.envVarKey` لمنع اختلاف مفتاح التوقيع بين الخدمتين.

التحقق المحلي الأخير: `go test ./...` و`go vet ./...` و`npm run test:run` (47 اختباراً) و`npm run build:check` نجحت. تم تطبيق `054_refresh_token_revocation` على قاعدة Supabase. أُعيد بناء Backend ثم بُني Installer وPortable لـWindows، وتحققت مطابقة Backend المضمّن داخل الحزمة مع النسخة الجديدة. اختبارات Smoke المحلية والسحابية للمسارات الصحية والحماية نجحت؛ المتبقي الخارجي هو تأكيد نشر Render وتوزيع Electron.
