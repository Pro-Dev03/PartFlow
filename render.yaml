services:
  # The API already exists as partflow-api.onrender.com and is managed
  # separately in Render. Keep it out of this Blueprint to avoid a duplicate.
  - type: keyvalue
    name: partflow-redis
    plan: free
    ipAllowList: []
    maxmemoryPolicy: noeviction

