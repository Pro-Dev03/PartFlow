# تقرير بنية PartFlow لتطبيق Windows والعمل دون اتصال

## 1. القرار المقترح

يوصى بتحويل PartFlow إلى تطبيق سطح مكتب Windows باستخدام Electron، مع الاحتفاظ بواجهة React وطبقة الأعمال في Go، وإضافة قاعدة بيانات محلية SQLite.

البنية المقترحة:

```text
Electron Desktop App
├── React UI
├── Go Backend محلي (Sidecar Process)
├── SQLite Database محلية
├── Sync Queue
└── Backup Manager

عند توفر الإنترنت:
Go Backend المحلي <── مزامنة آمنة ──> Go API المركزي <──> PostgreSQL
```

يعمل التطبيق محلياً دون إنترنت، وتصبح المزامنة مع الخادم المركزي ميزة إضافية عند توفر الاتصال.

## 2. لماذا SQLite؟

SQLite مناسبة لتطبيق متجر يعمل على جهاز Windows واحد للأسباب التالية:

- لا تحتاج إلى تثبيت أو تشغيل خدمة قاعدة بيانات منفصلة.
- تخزن البيانات في ملف واحد يمكن نسخه احتياطياً بسهولة.
- سريعة ومستقرة للاستخدام المحلي.
- تعمل دون إنترنت بالكامل.
- مناسبة للتوزيع داخل تطبيق Electron.
- تسمح باستخدام معاملات ACID لحماية المبيعات والدفعات والمخزون.

لا يوصى بتضمين PostgreSQL محلياً داخل التطبيق، لأن تشغيله وتحديثه ونسخه الاحتياطي أكثر تعقيداً، ولا يقدم فائدة ضرورية لمتجر يعمل على جهاز واحد.

## 3. توزيع المسؤوليات

### Electron

- تشغيل واجهة React.
- تشغيل وإيقاف Backend Go المحلي.
- مراقبة حالة Backend والاتصال.
- تنفيذ وظائف سطح المكتب مثل الطباعة والنسخ الاحتياطي.
- استخدام Electron IPC فقط للوظائف التي تحتاج صلاحيات سطح المكتب.

### React

- عرض الواجهات والبيانات.
- إظهار حالة الاتصال والمزامنة.
- استخدام نفس API المحلي تقريباً حتى لا تختلف تجربة الاستخدام بين الأوفلاين والأونلاين.

### Backend Go المحلي

- تنفيذ منطق الأعمال والتحقق من الصلاحيات.
- الاتصال بقاعدة SQLite.
- تسجيل كل عملية قابلة للمزامنة.
- إدارة طابور المزامنة وإعادة المحاولة.
- منع تكرار العمليات عند إعادة الإرسال.

### PostgreSQL المركزي

- حفظ نسخة مركزية عند استخدام عدة أجهزة أو الوصول عن بعد.
- استقبال العمليات المتزامنة.
- حفظ سجل المزامنة والتدقيق.
- توفير التقارير المركزية عند الحاجة.

## 4. قاعدة البيانات المحلية

يجب أن تكون SQLite متوافقة قدر الإمكان مع نموذج PostgreSQL الحالي، مع مراعاة اختلاف أنواع البيانات وبعض أوامر SQL.

يجب إضافة الحقول التالية إلى الجداول التي ستتم مزامنتها:

- `id`: UUID ثابت لا يتغير بين المحلي والمركزي.
- `created_at`: وقت الإنشاء.
- `updated_at`: وقت آخر تعديل.
- `deleted_at`: للحذف المنطقي عند الحاجة.
- `sync_status`: حالة المزامنة.
- `device_id`: الجهاز الذي أنشأ العملية.
- `version`: رقم إصدار السجل.

يجب استخدام UUID بدلاً من أرقام محلية متصاعدة حتى لا تتعارض السجلات بين الأجهزة.

## 5. تصميم المزامنة

لا تتم مزامنة الجداول كاملة في كل مرة. تتم مزامنة العمليات أو السجلات التي تغيرت فقط.

ينشأ جدول محلي مثل:

```sql
CREATE TABLE sync_queue (
    id TEXT PRIMARY KEY,
    entity_type TEXT NOT NULL,
    entity_id TEXT NOT NULL,
    operation TEXT NOT NULL,
    payload TEXT NOT NULL,
    created_at TEXT NOT NULL,
    attempts INTEGER NOT NULL DEFAULT 0,
    last_error TEXT,
    synced_at TEXT
);
```

تسلسل العملية:

1. ينفذ المستخدم البيع أو الدفع محلياً داخل معاملة واحدة.
2. تحفظ العملية في جداول الأعمال.
3. تسجل العملية في `sync_queue` داخل نفس المعاملة.
4. يظهر البيع فوراً للمستخدم حتى دون إنترنت.
5. يحاول عامل المزامنة إرسال العملية عند توفر الإنترنت.
6. يؤكد الخادم المركزي العملية باستخدام `idempotency_key`.
7. عند النجاح تُعلّم العملية كمزامنة.
8. عند الفشل يعاد الإرسال تدريجياً مع تسجيل الخطأ.

## 6. أولوية العمليات في المزامنة

يجب تنفيذ العمليات المالية بالترتيب التالي:

1. إنشاء البيع وعناصره.
2. تحديث حركة المخزون.
3. تسجيل الدفع أو الدين.
4. تسجيل المرتجع أو العكس.
5. تحديث التجميعات والتقارير.

لا ينبغي اعتبار العملية مكتملة مركزياً إذا تم إرسال جزء منها فقط. كل عملية مالية يجب أن تستخدم `idempotency_key` ومعاملة واحدة في الخادم المركزي.

## 7. معالجة التعارضات

المخزون والدفعات لا يعالجان بقاعدة "آخر تعديل يفوز"؛ هذه القاعدة قد تسبب خسارة مالية.

التوصية:

- المبيعات والدفعات والمرتجعات عمليات غير قابلة للتعديل بعد تسجيلها.
- التصحيح يتم بعملية عكس أو مرتجع جديد.
- تحديث المخزون يتم من خلال حركات مخزون غير قابلة للحذف.
- عند بيع نفس الكمية من جهازين دون اتصال، يرسل الخادم العملية الثانية إلى حالة تعارض بدلاً من قبول مخزون سالب بصمت.
- تعرض الواجهة التعارض للمستخدم مع خيار المراجعة والتسوية.
- بيانات الإعدادات غير المالية يمكن حل تعارضها باستخدام آخر إصدار مع تسجيل التدقيق.

## 8. الحسابات وتسجيل الدخول

في الوضع المحلي:

- يحتفظ التطبيق بحسابات المستخدمين داخل SQLite.
- يخزن كلمات المرور باستخدام تجزئة آمنة مثل Argon2 أو bcrypt، وليس كنص واضح.
- تحفظ جلسة الدخول محلياً بطريقة آمنة.
- لا تعتمد العمليات الأوفلاين على وجود خادم مركزي.

عند استخدام عدة أجهزة، يجب مزامنة المستخدمين والصلاحيات من الخادم المركزي وفق سياسة واضحة. لا يفضل السماح لكل جهاز بتغيير الصلاحيات أثناء عدم الاتصال.

## 9. النسخ الاحتياطي والاستعادة

يجب توفير وظائف واضحة داخل إعدادات التطبيق:

- نسخة احتياطية يدوية.
- نسخة احتياطية تلقائية يومية.
- الاحتفاظ بعدة نسخ سابقة.
- اختيار مكان حفظ النسخة.
- استعادة مع تأكيد واضح.
- فحص سلامة النسخة قبل الاستعادة.
- عدم تخزين النسخ الاحتياطية بجانب ملف البرنامج التنفيذي.

المسار المقترح:

```text
%APPDATA%/PartFlow/data/partflow.db
%APPDATA%/PartFlow/backups/
%APPDATA%/PartFlow/logs/
```

يجب استخدام SQLite Online Backup أو آلية نسخ آمنة، وعدم نسخ الملف أثناء وجود كتابة غير مكتملة.

## 10. التصفير وإدارة البيانات

خيار "تصفير البيانات" يجب أن يعمل على قاعدة SQLite المحلية في الوضع الأوفلاين، وعلى الخادم المركزي في الوضع الأونلاين حسب السياق.

التوصية:

- الحفاظ على حسابات الدخول المحلية.
- الحفاظ على جداول إصدارات الهجرة.
- حذف بيانات العمل داخل معاملة واحدة.
- منع التصفير أثناء وجود عمليات غير متزامنة، أو عرض تحذير واضح بشأنها.
- إنشاء نسخة احتياطية تلقائية قبل التصفير.
- تسجيل عملية التصفير في سجل محلي آمن.
- عند التصفير المركزي، تطبيق نفس السياسة على قاعدة PostgreSQL مع صلاحية مدير النظام فقط.

## 11. أمان تطبيق Electron

- عدم وضع أسرار PostgreSQL أو مفاتيح الخادم داخل Electron.
- تفعيل `contextIsolation` وتعطيل `nodeIntegration` في صفحات React.
- حصر Electron IPC في وظائف محددة.
- تشغيل Go Backend على `localhost` فقط.
- استخدام رمز جلسة محلي قصير العمر.
- تحديث التطبيق والتوقيع الرقمي للنسخ الإنتاجية.
- عدم تسجيل كلمات المرور أو رموز الجلسات في ملفات السجل.

## 12. خطة التنفيذ المرحلية

### المرحلة الأولى: فصل طبقة قاعدة البيانات

- تعريف واجهة Repository في Go.
- فصل منطق الأعمال عن PostgreSQL SQL المباشر.
- إضافة دعم SQLite خلف نفس الواجهات.
- مراجعة أنواع البيانات والاستعلامات غير المتوافقة.

### المرحلة الثانية: تشغيل Backend محلياً

- بناء Go Backend كملف Windows executable.
- تشغيله من Electron كـ Sidecar.
- إضافة فحص health check.
- ضبط مسارات البيانات والملفات حسب Windows.

### المرحلة الثالثة: دعم الأوفلاين

- إنشاء SQLite migrations.
- نقل العمليات الأساسية: المنتجات، المخزون، المبيعات، المشتريات، العملاء، الموردون، الديون، والدفعات.
- إضافة مؤشر حالة الاتصال.
- منع الواجهات من افتراض وجود الإنترنت.

### المرحلة الرابعة: المزامنة

- إنشاء `sync_queue`.
- إضافة `idempotency_key` لكل عملية مالية.
- إضافة endpoint للمزامنة على الخادم المركزي.
- إضافة إعادة المحاولة والتسجيل.
- بناء شاشة لمراجعة التعارضات.

### المرحلة الخامسة: التغليف والاختبار

- إعداد Electron Builder أو Forge.
- اختبار التثبيت والتحديث والإزالة.
- اختبار انقطاع الإنترنت أثناء البيع والدفع.
- اختبار إغلاق التطبيق أثناء عملية كتابة.
- اختبار استعادة النسخة الاحتياطية.
- اختبار جهازين ينفذان عمليات متعارضة.

## 13. الاختبارات الضرورية قبل الإصدار

- إنشاء بيع دون إنترنت.
- تسجيل دفعة دون إنترنت.
- إعادة تشغيل الجهاز بعد عملية غير متزامنة.
- عودة الإنترنت ومزامنة العملية مرة واحدة فقط.
- انقطاع الإنترنت أثناء المزامنة.
- فشل الخادم ثم إعادة المحاولة.
- تعارض مخزون بين جهازين.
- إنشاء نسخة احتياطية واستعادتها.
- تصفير البيانات مع بقاء حساب الدخول.
- ترقية قاعدة البيانات بين إصدارات التطبيق.

## 14. تجربة صاحب المتجر بعد التغليف

بعد تغليف النظام كتطبيق Windows عبر Electron، يجب أن تكون تجربة صاحب المتجر كالتالي:

1. يثبت التطبيق من خلال ملف Setup عادي.
2. يفتح PartFlow من اختصار سطح المكتب.
3. يسجل الدخول ويبدأ العمل دون تشغيل PostgreSQL أو Go أو Terminal.
4. يعمل التطبيق بشكل طبيعي عند انقطاع الإنترنت.
5. تحفظ المبيعات والمخزون والدفعات محلياً فوراً.
6. عند عودة الإنترنت تتم المزامنة تلقائياً في الخلفية.
7. تظهر حالة الاتصال والمزامنة برسائل بسيطة وواضحة.
8. ينشئ التطبيق نسخاً احتياطية تلقائية دون تدخل تقني.

يجب أن يتولى التطبيق تلقائياً تشغيل Backend المحلي، إنشاء قاعدة البيانات، تطبيق تحديثات قاعدة البيانات، إعادة المحاولة عند فشل المزامنة، وإظهار الأخطاء بلغة يفهمها صاحب المتجر.

لا ينبغي أن يحتاج المستخدم إلى معرفة مكان ملف SQLite أو تشغيل أوامر في Terminal أو إدارة خادم قاعدة بيانات.

عند استخدام جهاز Windows واحد تكون التجربة مباشرة بالكامل. أما عند استخدام عدة أجهزة، فيلزم إعداد أولي بسيط لربط الأجهزة بالحساب أو بالخادم المركزي، ثم تتم المزامنة تلقائياً بعد ذلك.

## 15. وضع الأوفلاين فقط

يجب دعم متجر يريد العمل دون إنترنت بشكل دائم من خلال خيار واضح في الإعدادات:

```text
وضع التشغيل:
[ أوفلاين فقط ] [ أونلاين ومزامنة ]
```

في وضع **أوفلاين فقط**:

- تكون SQLite المحلية هي مصدر البيانات الأساسي.
- لا يحتاج صاحب المتجر إلى خادم أو اتصال شبكي.
- تعمل المبيعات والمخزون والعملاء والموردون والدفعات محلياً.
- لا تظهر أخطاء اتصال مزعجة للمستخدم.
- يبقى النسخ الاحتياطي التلقائي واليدوي ضرورياً لحماية البيانات.

يكون وضع **أونلاين ومزامنة** اختيارياً لمن يريد استخدام عدة أجهزة أو الوصول إلى البيانات عن بُعد. يجب أن تسمح البنية بالترقية من الأوفلاين فقط إلى الأونلاين دون فقدان البيانات، وذلك عبر رفع نسخة البيانات المحلية ومزامنتها قبل تفعيل الحساب المركزي.

يفضل حفظ اختيار الوضع لكل جهاز بشكل مستقل، مع توفير إجراء واضح للترقية لاحقاً. ولا ينبغي إضافة زر تبديل شكلي قبل تنفيذ قاعدة محلية وطابور مزامنة حقيقيين.

## 16. الخلاصة

الاختيار الموصى به هو:

```text
Electron + React + Go Local Backend + SQLite
                         │
                         └── Sync عند توفر الإنترنت ──> Go API + PostgreSQL
```

هذا التصميم يحافظ على الاستثمار الحالي في React وGo وPostgreSQL، ويضيف تشغيل أوفلاين حقيقياً دون إدخال PostgreSQL داخل جهاز Windows. يجب تنفيذ المشروع على مراحل، والبدء بفصل طبقة قاعدة البيانات قبل بناء المزامنة.

أهم قاعدة: كل عملية بيع أو دفع أو حركة مخزون يجب أن تكون محلية وفورية، قابلة للمزامنة لاحقاً، وغير قابلة للتكرار أو الحذف الصامت.
